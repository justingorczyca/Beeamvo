name: beeamvo
description: "Beeamvo - Voice-to-text transcription app with global hotkey support"
publish_to: 'none'
version: 0.1.0

environment:
  # Keep this floor in sync with the resolved pubspec.lock (sdks.dart, currently
  # ">=3.12.0 <4.0.0"). Declaring ^3.10.4 here was looser than the actual minimum
  # the dependency graph requires, so a toolchain in the 3.10.x–3.11.x range
  # passed the manifest constraint but could not resolve the locked packages.
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  # Flutter's built-in localizations delegates (Material/Cupertino/Widgets).
  # Makes the app locale-aware at the framework level; app-string extraction
  # to .arb is a tracked follow-up (audit H12).
  flutter_localizations:
    sdk: flutter

  http: ^1.2.2
  hotkey_manager: ^0.2.3
  record: ^7.0.0
  super_clipboard: ^0.9.1
  path_provider: ^2.1.4
  path: ^1.9.0
  window_manager: ^0.5.2
  screen_retriever: ^0.2.2
  tray_manager: ^0.5.2
  win32: ^5.15.0
  ffi: ^2.1.4
  launch_at_startup: ^0.5.1
  package_info_plus: ^9.0.0
  google_fonts: ^8.2.1
  googleapis_auth: ^2.3.3
  flutter_secure_storage: ^10.3.1
  flutter_dotenv: ^6.0.0
  crypto: ^3.0.6
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.0

flutter_launcher_icons:
  image_path: "assets/app_icon.png"
  android: true
  ios: true
  windows:
    generate: true
    image_path: "assets/app_icon.png"
    icon_size: 48
  macos:
    generate: true
    image_path: "assets/app_icon.png"

flutter:
  uses-material-design: true

  assets:
    - assets/
